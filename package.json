{
  "name": "sre-leadership-case-study",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test .github/scripts/*.test.mjs",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "11.17.0",
    "marked": "18.0.13"
  }
}
